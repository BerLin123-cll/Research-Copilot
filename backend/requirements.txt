# ---- Web 框架 ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ---- 配置 / 校验 ----
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- 数据库 ----
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
aiosqlite>=0.20
psycopg2-binary>=2.9
alembic>=1.13
# pgvector：PostgreSQL 向量扩展（免费开源），向量库直接存储在 PostgreSQL 中
pgvector>=0.3

# ---- Agent 编排 ----
langgraph>=0.2.0
langchain-text-splitters>=0.2.0

# ---- LLM / Embedding ----
openai>=1.40

# ---- 本地开源免费 Embedding（fastembed + BAAI/bge 系列，ONNX 推理、无需 GPU/Key） ----
fastembed>=0.3.2

# ---- 工具 ----
duckduckgo-search>=6.2
arxiv>=2.1
httpx>=0.27
beautifulsoup4>=4.12
readability-lxml>=0.8
lxml>=5.2
pypdf>=4.2

# ---- 可选：动态网页渲染（启用 PLAYWRIGHT_ENABLED=true 时安装） ----
# playwright>=1.45
